# TODO List for GUI Updates

- [x] Update button sizes: Change height to 3 and width to 25 for all main buttons in main() function.
- [x] Add add_category_gui() function: Create a function to add new categories via a dialog, append to CATEGORIES list.
- [x] Add new button: Insert "إضافة قسم" button in main() function.
- [x] Change layout: Switch from pack() to grid() in main(), add a frame for buttons, increase window size to 700x600, add padding.
- [x] Test the GUI: Run the application to ensure changes work correctly.
